apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "microservices-with-helmchart.fullname" . }}-test-connection"
  labels:
    {{- include "microservices-with-helmchart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "microservices-with-helmchart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
